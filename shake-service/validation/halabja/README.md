# D20 Halabja smoke test — outputs

**Generator retired (2026-08-07, D20 checkpoint-outcome condition 3):**
this folder's outputs were originally generated by two event-hardcoded
scripts, `scripts/run_halabja.py` and `scripts/run_halabja_conditioned.py`
— both since deleted (git history preserves them) and superseded by the
generalized `scripts/run_validation.py --event <usgs-id>`, used for every
event validated afterwards (the 2018 Kermanshah pair,
`validation/us1000hwdw/`, `validation/us1000ghda/`; cross-event synthesis:
`validation/SUMMARY.md`). **This folder's files below are left exactly as
originally generated, NOT regenerated** — re-running the comparison via
`scripts/run_validation.py --event us2000bmcg --cache-dir .cache/halabja`
against the same cached USGS inputs was verified to reproduce the same
bias/coverage numbers bit-for-bit (checked this wave: bare PGA bias
-0.549/PGV -0.750, conditioned primary PGA 0.285/PGV -0.274, all four
sensitivity-grid rows), so nothing would be gained by churning this
already-Peshawa-reviewed, D20-cited artifact — only its *generator* changed.
The re-run writes the new unified single-report format to
`validation/us2000bmcg/` if anyone wants to see it in the new shape; this
folder keeps the original two-report (`REPORT.md` + `REPORT-CONDITIONED.md`)
shape Peshawa actually reviewed.

Originally generated by `scripts/run_halabja.py`, the D20 gate
(`docs/decisions.md` D20; `docs/research/gmpe-set-proposal-v2.md`
§7): our bare, unconditioned `gmpe_forward` prior vs. USGS's
station+DYFI-conditioned Atlas ShakeMap for the 2017 M7.3
Halabja/Sarpol-e Zahab earthquake (`us2000bmcg`).

- `REPORT.md` — the human-readable verdict, per criterion, plus
  interpretation. **Start here.**
- `results.json` — the same numbers, machine-readable (stats + config +
  versions), for anyone re-running the comparison later and diffing.
- `ems_map.png` — our forward EMS-98 map.
- `residual_map.png` — ln-PGA residual (ours − USGS) at every compared
  USGS grid cell.
- `distance_binned_bias.png` — PGA/PGV ln-bias by distance bin, with the
  D20 ±0.3 threshold lines.

Re-run (new shape, `validation/us2000bmcg/`, NOT overwriting this folder):
`./.venv/bin/python scripts/run_validation.py --event us2000bmcg --cache-dir .cache/halabja`
from `shake-service/` (reuses the cached USGS Atlas products under
`.cache/halabja/` — not committed, ~27 MB — fetched fresh only if that
cache is absent). Parameterization is covered by
`tests/test_run_validation.py` (mocked-event unit tests, no network); a
real-event pipeline run is not pytest-gated (a legitimate scientific FAIL
is not a test failure — same policy the retired `test_halabja_smoke.py`
stated).

## Wave D — the CONDITIONED re-judgment (`docs/decisions.md` "D20
smoke-test checkpoint outcome", 2026-08-06)

The bare-prior comparison above FAILED bias (both IMTs) and PGV coverage.
Peshawa's ruling: condition our own prior on the SAME Atlas DYFI
observations via our own `mvn.py`, then re-judge — a like-for-like
"conditioned vs. conditioned" comparison instead of "bare vs. conditioned".
Originally generated by `scripts/run_halabja_conditioned.py` (retired, see
top of this file):

- `REPORT-CONDITIONED.md` — the re-judgment: bare-prior-vs-conditioned D20
  table, distance-binned nuance, the full sensitivity grid (GMICE model /
  nresp floor / observation-domain choices), the DYFI-observation
  "doughnut", and the circularity caveat. **Start here.**
- `results-conditioned.json` — the same numbers, machine-readable, for
  every sensitivity variant plus the wave-C bare-prior baseline inlined for
  diffing.
- `ems_map_conditioned.png` — the CONDITIONED (primary-variant) EMS-98 map.
- `residual_map_conditioned.png` — ln-PGA residual (conditioned − USGS).
- `distance_binned_bias_before_after.png` — bare (wave C) vs. conditioned
  (wave D) PGA/PGV ln-bias by distance bin, side by side.

Headline: the PRIMARY conditioned variant (Worden/MMI GMICE, nresp>=3,
observations restricted to our own grid's magnitude-scaled domain) PASSES
all four D20 criteria. Every alternative choice (Zanini/EMS GMICE,
nresp>=2, or an unrestricted global observation domain) is also computed
and reported — some fail — so the pass is shown as real but not
knife-edge-fragile-proof; read `REPORT-CONDITIONED.md`'s own sensitivity
grid and circularity-caveat sections before treating this as a clean bill
of health. Re-run (new shape): same `scripts/run_validation.py --event
us2000bmcg --cache-dir .cache/halabja` command as above — one run now
computes both the bare and conditioned comparisons together.
